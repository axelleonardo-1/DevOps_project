{
  "name": "ecommerce-orders-service",
  "version": "2.0.0",
  "description": "Orders service migrated to Hono and Cloudflare Workers",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --config wrangler.toml --env dev --ip 0.0.0.0 --port 3002",
    "deploy": "wrangler deploy --config wrangler.toml --env production",
    "test": "vitest run --coverage",
    "test:watch": "vitest"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "hono": "^4.8.5"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^3.2.4",
    "vitest": "^3.2.4",
    "wrangler": "^4.16.1"
  }
}
