name: Orders CI/CD

on:
  pull_request:
    branches:
      - develop
      - main
  push:
    branches:
      - develop
      - main

jobs:
  ci:
    name: Orders CI
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: services/orders

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: services/orders/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Run lint if available
        shell: bash
        run: |
          if node -e "process.exit(require('./package.json').scripts?.lint ? 0 : 1)"; then
            npm run lint
          else
            echo "No lint script defined for services/orders"
          fi

      - name: Run unit tests
        run: npm test

      - name: Run build if available
        shell: bash
        run: |
          if node -e "process.exit(require('./package.json').scripts?.build ? 0 : 1)"; then
            npm run build
          else
            echo "No build script defined for services/orders"
          fi

      - name: Run typecheck if available
        shell: bash
        run: |
          if node -e "process.exit(require('./package.json').scripts?.typecheck ? 0 : 1)"; then
            npm run typecheck
          else
            echo "No typecheck script defined for services/orders"
          fi

  deploy-development:
    name: Deploy Orders to Development
    if: github.event_name == 'push' && github.ref == 'refs/heads/develop'
    needs: ci
    runs-on: ubuntu-latest
    environment:
      name: development
    defaults:
      run:
        working-directory: services/orders

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: services/orders/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Resolve shared Cloudflare bindings
        id: bindings
        shell: bash
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          D1_DATABASE_NAME: devops-ecommerce-dev
          PRODUCTS_SERVICE_URL: ${{ secrets.PRODUCTS_SERVICE_URL_DEV }}
        run: |
          if [ -z "${PRODUCTS_SERVICE_URL}" ]; then
            echo "Missing PRODUCTS_SERVICE_URL_DEV secret."
            exit 1
          fi

          D1_DATABASE_ID="$(npx wrangler d1 list --json | node -e "let data='';process.stdin.on('data', chunk => data += chunk);process.stdin.on('end', () => { const databases = JSON.parse(data); const match = databases.find((database) => database.name === process.env.D1_DATABASE_NAME); if (!match) { console.error('Missing D1 database: ' + process.env.D1_DATABASE_NAME); process.exit(1); } process.stdout.write(match.uuid || match.id || ''); });")"

          if [ -z "${D1_DATABASE_ID}" ]; then
            echo "Failed to resolve the development D1 database ID."
            exit 1
          fi

          echo "d1_database_id=${D1_DATABASE_ID}" >> "$GITHUB_OUTPUT"
          echo "d1_database_name=${D1_DATABASE_NAME}" >> "$GITHUB_OUTPUT"
          echo "products_service_url=${PRODUCTS_SERVICE_URL}" >> "$GITHUB_OUTPUT"

      - name: Render Wrangler config
        env:
          D1_DATABASE_ID: ${{ steps.bindings.outputs.d1_database_id }}
          D1_DATABASE_NAME: ${{ steps.bindings.outputs.d1_database_name }}
          PRODUCTS_SERVICE_URL: ${{ steps.bindings.outputs.products_service_url }}
        run: node ../../scripts/render-wrangler-config.mjs orders dev wrangler.ci.toml

      - name: Apply shared D1 migrations
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
        run: npx wrangler d1 migrations apply DB --remote --env dev --config wrangler.ci.toml

      - name: Deploy Worker to Cloudflare development
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
        run: npx wrangler deploy --env dev --config wrangler.ci.toml
